# Assignment 2 本机环境

工作区：`/Users/mac/cyy/Games101`。使用 Apple Clang、CMake、Eigen、OpenCV 和 C++17。
CMake 自动查找 Eigen 和 OpenCV，构建输出位于工作区的 `build/Assignment2`。

## VS Code

请打开整个 `/Users/mac/cyy/Games101` 文件夹。

- `Cmd + Shift + B`：配置并编译 A2。
- 在“运行和调试”中选择 `Assignment2 (CMake)`，按 `F5`：编译并启动窗口调试；窗口获得焦点后按 `Esc` 退出。
- 选择 `Assignment2 (PNG)`，按 `F5`：生成 `build/Assignment2/output.png` 并退出，支持断点调试。
- Code Runner 的 C++ 运行按钮已指向 A2。
- A0、A1 的构建任务和调试入口仍可从列表选择。

头文件补全使用 CMake Tools 和 `build/Assignment2/compile_commands.json`。
如果已打开的 VS Code 仍使用 A1 配置，执行 `CMake: Configure`；必要时执行 `Developer: Reload Window`。

## 终端

```bash
cd /Users/mac/cyy/Games101
cmake -S "GAMES101-Homework/Assignment2/代码框架" -B build/Assignment2 -DCMAKE_BUILD_TYPE=Debug -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build/Assignment2 --parallel
./build/Assignment2/Rasterizer
```

直接生成图片：

```bash
./build/Assignment2/Rasterizer ./build/Assignment2/output.png
```

## 作业状态

环境已配置，作业算法仍待完成，当前输出为黑图。

- `main.cpp` 的 `get_projection_matrix()`：保留 TODO，目前以单位矩阵安全占位，后续需迁入 A1 的投影实现。
- `rasterizer.cpp` 的 `insideTriangle()`：保留 TODO，目前返回 `false` 占位，后续需实现点在三角形内的判断。
- `rasterize_triangle()`：仍需实现包围盒、像素遍历、深度测试和颜色写入。

## 本机验证（2026-09-12）

- Debug 配置、编译和链接通过，编译数据库包含 Eigen、OpenCV 头文件目录。
- 命令行生成 700 × 700 RGB PNG，退出码为 0。
- LLDB 成功停在 `main.cpp` 的断点，继续运行后退出码为 0。
- 正常桌面权限下，窗口模式持续输出帧计数，确认已进入显示和键盘事件循环；验证后已停止测试进程。
- 已安装 VS Code CMake Tools、C/C++ 和 CodeLLDB；本次调试验证使用命令行 LLDB。
